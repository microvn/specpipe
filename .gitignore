# Dependencies
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# npm publish artifacts (generated by prepublishOnly, deleted by postpublish)
cli/templates/
cli/README.md

# Environment
.env*
!.env.example

# Debug logs
*.log

# Test artifacts (created by test/hooks.sh)
.test-agentignore-*/
.test-aiignore-*/

# Vercel landing page (deployed separately, not versioned here)
site/

# Internal R&D / client-specific — NEVER published to the public OSS repo.
# These reference private client work (Vantage / agentfolk), real bug IDs,
# QA staff names, and local credentials. Kept on disk, out of git.
test/real-replay/
test/qa-benchmark/
test/qa-benchmark.sh
docs/qa-audit-*.md
docs/sibling-drift-*.md

# Scratch comparison clones — superseded by the ported originals in kit/skills/sp-*
kit/skills/*-behavior-matrix/
